class Games {
  int id;
  String? name;
  String? level;
  String? percentage;
  int? price;
  String? content;

  Games(this.id,
      {this.name, this.level, this.percentage, this.price, this.content});
}
